
# 连续多单与 YOLO 记忆矩阵改动说明

1. 本次新增改动主要集中在 `snapshot_pick_client.py`，对应的 GUI 可视化程序为 `gui_snapshot_pick.py`。

2. 本次实现了一个**简化版记忆矩阵机制**。机器人在任务执行过程中可以持续记录已经观察到的各个货架及其货物信息，并将其保存到记忆矩阵中。启用 `gui_snapshot_pick.py` 后，可以实时查看记忆矩阵在整个任务过程中的更新和变化。

3. 记忆矩阵的信息录入**不需要额外执行专门的货架扫描动作**。机器人在正常的抓取、移动和送货过程中，会利用途中获得的视觉信息顺带更新记忆矩阵，从而减少额外感知动作带来的时间开销。

4. 目前记忆矩阵主要解决的是**货物信息获取和重复扫描的问题**，还没有改变最终的抓取方式。机器人确定目标货物所在货架后，仍然需要导航到对应货架的中心点，并进行后续姿态调整，暂时还无法做到在移动途中发现目标后直接停车并立即抓取。

5. 该机制目前的主要作用是**减少逐个货架扫描的时间，以及减少到达单个货架后对整个货架进行完整扫描的时间**。如果记忆矩阵中已经记录了目标货物的位置，就可以直接利用已有信息进行后续导航和抓取，而不需要重新扫描。

6. 目前仍有一个初始化阶段的特殊情况：**第一轮和第二轮任务仍然需要像原方案一样分别主动扫描 E 货架和 A 货架。**第一轮需要扫描 E 货架；由于第一轮送货过程中的视角存在盲区，无法可靠获取 A 货架的信息，因此第二轮仍需要主动扫描 A 货架。完成这两轮后，后续货架信息主要依靠抓取和送货过程中的顺带观测来持续补充和更新记忆矩阵。

## 后续完善

7. 记忆矩阵采用纯 YOLO 的 3×15 固定槽位结构。单个画面可以根据检测结果的全局坐标同时记录多个货架；矩阵持续接收观测，并优先保留机器人距离商品更近时获得的可靠记录。

8. 导航提示只使用“货架＋层”，列信息不参与导航。导航读取的是矩阵当前主记录，与可视化中显示的内容保持一致，被覆盖的隐藏历史候选不会单独触发导航。

9. 商品确认离架后立即消费矩阵记录，不再等到送货并返回货架区后才更新。消费按“货架＋层＋品类”处理，即使抓取定位列与矩阵记录列存在漂移，也能及时移除已取走商品；后续新的 YOLO 观测仍可重新建立真实库存记录。

10. 矩阵提示的货架和层核验失败后，会立即排除该提示并选择下一条可靠矩阵记录。例如 B 货架的薯片提示失败后，如果矩阵记录指向 D，机器人会直接前往 D，不再先扫描没有薯片记录的 C。已经完整扫描失败的货架也不会在同一订单中被异常回访。

11. 记忆矩阵与 GUI 没有运行依赖关系。`MemoryMatrixTracker` 在客户端进程中维护矩阵，并写入 `logs/memory_matrix.json`；GUI 只负责启动容器、显示日志和可视化 JSON。`snapshot_pick_client.py` 可以完全通过命令行运行。

12. ArUco 不参与记忆矩阵录入和矩阵导航，仍只保留在抓取动作阶段用于必要的精细调整。

## 验证情况

- 记忆导航专项测试：8/8 通过。
- 记忆矩阵专项测试：14/14 通过。
- 近距离抓取复检测试：6/6 通过。
- 完整 Client 测试共 59 项，其中 56 项通过；剩余 3 项为本次修改前已经存在、与记忆矩阵改动无关的失败。
