# 第一次探测：带终点朝向约束的进站路径实验

## 1. 文件说明

本文件是“第一次探测进站路径优化”新增的实验说明文件。

对应代码：

- `examples/supermarket_sorting/supermarket_navigation.py`：在原有导航器中加入可选的终点朝向约束。
- `examples/supermarket_sorting/integrated_nav_pick_place_terminal_first_scan.py`：只在第一次 `GO_SCAN` 阶段启用优化，并负责统一计时。
- `tests/test_first_scan_terminal_approach.py`：验证终点路径、回退机制和实验开关。

这项实验优化的是路径几何形状，不是提高速度参数。最大速度、加速度、避障距离、代价地图膨胀半径和刹车逻辑均保持原值。

## 2. 优化目标

原始导航把扫描站当成带最终朝向的点目标。机器人可能先到达目标附近，再原地旋转到扫描朝向，导致第一次进站耗时增加。

优化方案在目标前增加一段与最终朝向一致的直线终端路径。以本次第一扫描站为例：

```text
扫描目标：(1.80, 2.475, 90°)
终端段长度：0.65 m
终端段起点（anchor）：(1.80, 1.825)

A* 路径 → (1.80, 1.825) → 沿目标朝向前进 → (1.80, 2.475, 90°)
```

机器人在最后 `0.65 m` 的平移过程中逐渐取得扫描所需朝向，从而减少到站后的单独旋转和反复修正。
### 2.1 一句话理解这个优化

可以把扫描站想象成一个停车位，把最后的 `0.65 m` 想象成停车位前的“进站跑道”。

这项优化不是让机器人多走 `0.65 m`，也不是提高机器人速度，而是要求机器人在距离扫描目标还有 `0.65 m` 时，就开始沿最终扫描朝向进入目标。

### 2.2 原始方案和优化方案的区别

原始方案主要关注到达目标位置：

```text
起点 → A* 路径 → 从任意方向到达扫描点 → 原地旋转到扫描朝向 → SCAN
```

机器人可能斜着到达扫描点。由于底盘不能横向移动，到达后通常还需要减速、原地旋转、修正位置并再次修正朝向。

优化方案为目标增加一条有方向的终端路径：

```text
起点 → A* 路径 → 进站点 → 沿最终朝向前进 0.65 m → 扫描点 → SCAN
```

机器人在最后一段向前移动的同时完成朝向调整，到达扫描点时已经基本面对正确方向，因此可以减少到站后的原地旋转和反复修正。

也可以把两种方案概括为：

```text
基线：先到位置，再调整方向。
优化：在前往位置的过程中，同时调整方向。
```

### 2.3 为什么距离为 0 是基线，0.65 m 是优化

终端距离表示“有方向的最后一段路径有多长”。

当终端距离为 `0` 时，进站点与扫描目标重合，没有可以约束进入方向的终端路径，所以代码将功能视为关闭：

```text
distance = 0 → mode = disabled → 基线方案
```

当终端距离为 `0.65 m` 时，系统会在目标沿最终朝向的反方向设置一个进站点，形成长度为 `0.65 m` 的定向终端路径：

```text
distance = 0.65 → mode = active → 优化方案
```

`0.65 m` 不是额外增加的固定路程。A* 会重新规划到进站点，实际总路径可能略有变化；节省的主要是终点附近的原地旋转和姿态反复修正时间。

距离也不是越大越好：太短可能来不及形成正确朝向，太长可能增加绕行或更容易被障碍物阻挡。`0.65 m` 是本实验验证过的当前参数。

### 2.4 另外两个参数的简单含义

- `MERGE_AHEAD=0.30`：如果途中重新规划，就从机器人前方约 `0.30 m` 重新接入终端路径，避免倒退回最初的进站点。
- `RELEASE_MARGIN=0.12`：距离目标很近时解除终端路径约束，让原有控制器完成最后的位置和朝向微调。

### 2.5 为什么不提高速度也能缩短时间

差速底盘不能直接横向移动，进入目标的方向会显著影响最后的调整时间。这项优化把“向前移动”和“取得最终朝向”尽量同时完成，因此即使最大速度和加速度完全不变，也可以更早进入 `SCAN`。

如果终端路径被障碍物阻挡，系统会自动回退到原有 A* 方案，不会为了追求速度而降低避障安全标准。

## 3. 具体如何优化

### 3.1 终端路径生成

当终点朝向约束启用时，导航器按照目标位置 `(goal_x, goal_y)`、目标朝向 `goal_yaw` 和终端距离 `distance` 计算终端段起点：

```text
anchor_x = goal_x - distance × cos(goal_yaw)
anchor_y = goal_y - distance × sin(goal_yaw)
```

导航器先使用原有 A* 规划到终端段的接入点，再把最终目标追加到路径末尾。

### 3.2 重规划时不倒退

如果机器人已经越过最初的终端段起点，重新规划时不会要求机器人返回固定起点，而是在机器人当前位置前方选择新的接入点。默认向前移动 `0.30 m`，避免出现短距离倒车或来回摆动。

### 3.3 接近目标时解除约束

当机器人已经接近终点，或在终端段上的进度达到末端范围时，约束会切换为：

```text
mode = released_near_goal
```

随后由原有的位置容差和朝向容差完成最后对准。默认解除余量为 `0.12 m`。

### 3.4 障碍物和失败回退

终端段不会绕过原有安全检查：

- 接入点必须位于可通行区域。
- 接入点到目标之间的直线必须通过代价地图碰撞检查。
- 如果接入点被障碍物占据、终端段被阻挡或接入点不可达，自动回退到原有直接 A* 路径。
- 激光雷达、深度避障、停止距离、恢复行为和刹车逻辑继续生效。
- 启用终点朝向实验时不恢复旧的路径缓存，避免旧路径绕过新的终端段。

## 4. 第一次探测路径时间的测量标准

计时使用 Python 的 `time.monotonic()`，单位为秒。它是单调递增的真实经过时间，不受系统时钟校准影响。

### 4.1 什么时候开始计时

第一次进入初始 `GO_SCAN` 导航阶段，并同时满足以下条件时开始：

- 当前流程是抓取流程：`flow_phase == "grab"`。
- 当前状态是前往扫描站：`state == STATE_GO_SCAN`。
- 尚未识别到目标标记：`target_marker_id is None`。
- 已收到机器人底盘位置：`base_xy` 有效。
- 已收到关节状态：`joints` 有效。
- 这是任务中的第一次扫描移动，计时尚未完成。

满足条件后，worker 启用终点朝向配置，并立即记录：

```python
started_at = time.monotonic()
```

基线和优化版本都使用同一个 worker 和同一个计时起点。基线只是把终端距离设为 `0`，不会使用另一套计时代码。

### 4.2 什么时候停止计时

父控制器执行完一次 `tick()` 后，如果状态从 `GO_SCAN` 进入 `SCAN`，立即停止计时：

```python
elapsed_s = time.monotonic() - started_at
```

进入 `SCAN` 表示：

- 机器人已经到达第一扫描站的位置范围；
- 底盘朝向已经满足扫描要求；
- 相机姿态已经完成并稳定；
- 控制器准备开始实际扫描。

日志中的停止标志是：

```text
[first-scan-terminal] first scan station reached elapsed=24.615s
```

### 4.3 计时包含什么

计时结果包含：

- 第一次路径规划和必要的重新规划；
- 从初始位置移动到第一扫描站的真实运动时间；
- 避障、减速、最终位置和朝向对准；
- 相机姿态稳定所需时间；
- 到达前发生的 stale feedback 暂停。

因此，如果 `first scan station reached` 之前出现：

```text
stopping for stale robot feedback
```

这次暂停会被计入 `elapsed`，分析 A/B 结果时必须同时记录。

### 4.4 计时不包含什么

计时不包含：

- worker 启动、模型加载以及第一批底盘/关节反馈就绪前的时间；
- 进入 `SCAN` 后的目标检测、识别和扫描循环；
- 抓取、放置、配送以及后续扫描路径；
- `first scan station reached` 日志之后出现的 stale feedback。

如果程序在进入 `SCAN` 前被停止，只能得到未完成的临时时间，不能作为有效实验结果。有效结果必须出现 `first scan station reached`，或结果字段中 `completed` 为 `true`。

## 5. A/B 对比标准

为保证基线与优化使用完全相同的计时器，两边都运行：

```text
examples/supermarket_sorting/integrated_nav_pick_place_terminal_first_scan.py
```

唯一的实验变量是终端段距离：

```bash
# 基线：关闭终点朝向路径
SUPERMARKET_FIRST_SCAN_TERMINAL_DISTANCE=0

# 优化：启用 0.65 m 终端段
SUPERMARKET_FIRST_SCAN_TERMINAL_DISTANCE=0.65
```

其余固定参数：

```bash
SUPERMARKET_FIRST_SCAN_TERMINAL_MERGE_AHEAD=0.30
SUPERMARKET_FIRST_SCAN_TERMINAL_RELEASE_MARGIN=0.12
SUPERMARKET_PATH_MEMORY=0
SUPERMARKET_SEED=42
SUPERMARKET_OBSTACLE_SEED=1000045
SUPERMARKET_TASKS=product_001
```

每次测试还应遵守：

1. 基线和优化使用同一个 Docker 镜像、任务和随机种子。
2. 每次运行前重启模拟服务器。
3. 重启后使用相同的固定预热时间，本实验为 `40 s`。
4. 到达第一次扫描站后立即停止客户端，避免完整任务长期运行导致模拟速度发生变化。
5. 交替运行顺序，例如第1轮“基线→优化”、第2轮“优化→基线”。
6. 同时记录到达前的 stale feedback 次数。

## 6. 日志判定

基线正常关闭约束时：

```text
[first-scan-terminal] plan={"enabled": false, "mode": "disabled"}
```

优化路径正常启用时：

```text
[first-scan-terminal] plan={"enabled": true, "mode": "active", ...}
[first-scan-terminal] plan={"enabled": true, "mode": "released_near_goal", ...}
```

提取计时和 stale 日志：

```bash
grep -E 'first-scan-terminal.*(plan=|first scan station reached)|stale robot feedback' client.log
```

## 7. 最新版三轮实测结果

测试目录：`results/latest_terminal_heading_ab/`

| 轮次 | 基线 | 优化后 | 节省时间 | 提升比例 | 到达前 stale（基线/优化） |
|---|---:|---:|---:|---:|---:|
| 第1轮 | 28.436 s | 24.615 s | 3.821 s | 13.44% | 1 / 1 |
| 第2轮 | 27.170 s | 24.305 s | 2.865 s | 10.54% | 1 / 0 |
| 第3轮 | 27.423 s | 24.906 s | 2.517 s | 9.18% | 1 / 1 |
| 平均 | 27.676 s | 24.609 s | 3.068 s | 11.08% | — |

三轮中优化方案全部更快。优化时间范围为 `24.305–24.906 s`，基线时间范围为 `27.170–28.436 s`。该结果说明带终点朝向约束的进站路径在当前固定种子场景下有效，但若需要统计意义更强的结论，应增加种子数量和重复次数。

## 8. 测试命令

```bash
python3 -B tests/test_first_scan_terminal_approach.py
python3 -B tests/test_supermarket_navigation.py
```

当前合并版本的测试结果：

- 终点朝向测试：6 项通过。
- 导航测试：30 项通过。
- 完整单元测试：71 项通过，8 项按环境条件跳过。